import React,{useEffect,useMemo,useState}from'react';
import {Helmet}from'react-helmet-async';
import CanopyAssignmentsV2 from './CanopyAssignmentsV2';
import CanopyCanvas from './CanopyCanvas';
import CanopyNotifications from './CanopyNotifications';
import CanopyCertificate from './CanopyCertificate';
import {CANOPY_ASSIGNMENT_SCHEDULE} from './canopySchedule';
import CantestApp from '../cantest/CanopyApp';
import{ArrowLeft,ArrowRight,BookOpen,Check,ChevronRight,ClipboardCheck,Clock,FileText,GraduationCap,Leaf,Lock,LogOut,Menu,PlayCircle,Sparkles,UserRound,X,BookMarked,UsersRound,PenLine,TrendingUp,Eye,EyeOff,MessageSquare,ShieldAlert,Award,BarChart3,Bell} from 'lucide-react';
import'./canopy.css';
import{CANOPY_BRAND,modules,resources}from'./canopyData';
import{canopyConfigured,consumeAuthCallback,getStoredSession,getViewer,getProgress,getSubmissions,getManagerSnapshot,markLesson,requestPasswordReset,resendConfirmation,saveQuiz,signIn,signOut,signUp,submitAssignment,updatePassword,setLearnerEnrollmentStatus,createManagerAction,updateManagerAction} from './canopyApi';

const route=()=>window.location.pathname.replace(/\/$/,'')||'/canopy';
function go(path){window.history.pushState({},'',path);window.dispatchEvent(new PopStateEvent('popstate'));window.scrollTo({top:0,behavior:'smooth'})}
const totalLessons=modules.reduce((n,m)=>n+m.lessons.length,0);
const canopyDisplayWeek=w=>String(w).replace(/Weeks?\s*1(?:–|–|-)2/i,'Week 1 & 2').replace(/Weeks?\s*5(?:–|–|-)6/i,'Week 5 & 6').replace(/Weeks?\s*7(?:–|–|-)8/i,'Week 7 & 8');

function Brand({compact=false}){return <button className={'canopyBrand '+(compact?'compact':'')} onClick={()=>go('/canopy')} aria-label="WOMATE Canopy home"><img src="/assets/canopy/canopy-logo-primary.png" alt="Canopy — Climate Learning by WOMATE"/></button>}
function PublicTop(){return <header className="canopyPublicTop"><Brand/><button className="canopyTextBtn" onClick={()=>go('/canopy/login')}>Participant sign in <ArrowRight size={16}/></button></header>}
function Meta(){return <div className="canopyMeta"><span>{CANOPY_BRAND.duration}</span><span>{CANOPY_BRAND.level}</span><span>{CANOPY_BRAND.cost}</span><span>Cohort-paced + guided</span></div>}
function Landing(){return <div className="canopyPublic"><PublicTop/><main>
  <section className="canopyLandingHero"><div className="canopyHeroCopy"><h1>Climate learning that grows into leadership.</h1><p>Canopy is WOMATE's learning environment for structured, practical climate education.</p><div className="canopyActions"><button className="canopyPrimary" onClick={()=>go('/canopy/login')}>Enter Canopy <ArrowRight size={17}/></button><button className="canopySecondary" onClick={()=>document.getElementById('canopy-course')?.scrollIntoView({behavior:'smooth'})}>Explore the course</button></div></div><div className="canopyHeroVisual" aria-hidden="true"><div className="canopyRings"><span/><span/><span/></div><CanopyHeroDemo/></div></section>
  <section className="canopyCourseIntro canopyCourseIntroEditorial" id="canopy-course"><div className="canopyCourseIntroLead"><span className="canopyEyebrow">SHE LEADS · COHORT 2</span><h2>A foundational climate leadership journey for young women who want to gain the knowledge, skills and courage to lead climate action.</h2></div><Meta/></section>
  <section className="canopyModulePreview">{modules.map(m=><details className="canopyModuleAccordion" key={m.id}><summary><span className="canopyModuleNo">{m.number}</span><div><small>{canopyDisplayWeek(m.week)}</small><h3>{m.title}</h3></div><ChevronRight className="canopyModuleChevron"/></summary><div className="canopyModuleAnswer"><p>{m.question}</p></div></details>)}</section>
  <section className="canopyHow"><div><span className="canopyEyebrow">HOW IT WORKS</span><h2>Learn. Meet. Apply. Move forward.</h2></div><div className="canopyHowIcons"><article><div className="canopyHowIcon"><BookMarked/></div><div><h3>Learn</h3><p>Short foundational lessons turn each topic into clear, manageable concepts.</p></div></article><article><div className="canopyHowIcon"><UsersRound/></div><div><h3>Meet</h3><p>Join the Thursday live session to learn directly from an expert speaker for the module.</p></div></article><article><div className="canopyHowIcon"><PenLine/></div><div><h3>Apply</h3><p>Complete the main assignment from Thursday through Sunday while the module is active.</p></div></article><article><div className="canopyHowIcon"><TrendingUp/></div><div><h3>Advance</h3><p>Progress with the cohort as the next module opens according to the programme schedule.</p></div></article></div></section>
 </main><footer className="canopyPublicFooter"><div className="canopyFooterBrand"><img src="/assets/canopy/canopy-logo-primary.png" alt="Canopy — Climate Learning by WOMATE"/></div><div className="canopyFooterMeta"><span>Driving inclusive climate action with technology.</span><span>WOMATE learning environment</span></div></footer></div>}
function Auth({mode='login'}){
 const[form,setForm]=useState({full_name:'',country:'',email:'',password:''});
 const[busy,setBusy]=useState(false);
 const[msg,setMsg]=useState('');
 const[msgType,setMsgType]=useState('info');
 const[showPassword,setShowPassword]=useState(false);
 const feedback=(text,type='info')=>{setMsg(text);setMsgType(type)};
 const submit=async e=>{e.preventDefault();setBusy(true);feedback('');try{
   if(mode==='login'){
     await signIn(form.email,form.password);
     feedback('Signed in successfully. Opening Canopy…','success');
     window.location.assign('/canopy/classroom')
   }else{
     const data=await signUp(form);
     if(data?.access_token){
       feedback('Account created and signed in. Canopy learning access opens from 1 October 2026.','success');
       go('/canopy/classroom')
     }else if(data?.possible_existing_account){
       feedback('An account may already exist for this email. Try signing in, or use “Resend confirmation” if the account is still unconfirmed.','warning')
     }else{
       feedback('Account created. We sent a confirmation email. Open your newest email from WOMATE and confirm your address, then return here to sign in.','success')
     }
   }
 }catch(err){
   const text=String(err?.message||'Unable to continue.');
   feedback(text,/confirm|verify/i.test(text)?'warning':'error')
 }finally{setBusy(false)}};
 const resend=async()=>{setBusy(true);feedback('');try{await resendConfirmation(form.email);feedback('A fresh confirmation email has been sent. Use the newest link only; older confirmation links may no longer work.','success')}catch(err){feedback(err.message,'error')}finally{setBusy(false)}};
 const forgot=async()=>{setBusy(true);feedback('');try{await requestPasswordReset(form.email);feedback('If an account exists for this email, a password-reset link has been sent. Check your inbox and spam folder.','success')}catch(err){feedback(err.message,'error')}finally{setBusy(false)}};
 return <div className="canopyAuth canopyAuthLuxe">
  <Helmet><title>{mode==='login'?'Sign in':'Create account'} | WOMATE Canopy</title></Helmet>
  <aside className="canopyAuthVisual">
   <div className="canopyAuthVisualOverlay"/>
   <button className="canopyAuthLogo" onClick={()=>go('/canopy')} aria-label="Canopy home"><img src="/assets/canopy/auth/canopy-logo-white.png" alt="Canopy — Climate Learning by WOMATE"/></button>
   <div className="canopyAuthVisualCopy">
    <span className="canopyAuthKicker">SHE LEADS · COHORT 2</span>
    <h1>{mode==='login'?'Continue your climate leadership journey.':'Your climate learning starts here.'}</h1>
    <p>{mode==='login'?'Return to your lessons, live sessions, assignments and progress in Canopy.':'Create your WOMATE learning account and get ready for She Leads Cohort 2.'}</p>
   </div>
   <div className="canopyAuthVisualFoot"><span>UNDERSTAND</span><span>QUESTION</span><span>ENGAGE</span><span>LEAD</span></div>
  </aside>
  <main className="canopyAuthPanelWrap">
   <button className="canopyBack" onClick={()=>go('/canopy')}><ArrowLeft/> Back to Canopy</button>
   <section className="canopyAuthPanel">
    <div className="canopyAuthHead">
     <span>{mode==='login'?'PARTICIPANT ACCESS':'CREATE ACCOUNT'}</span>
     <h2>{mode==='login'?'Welcome back':'Join Canopy'}</h2>
     <p>{mode==='login'?'Sign in to continue your She Leads learning journey.':'Create your secure Canopy account to begin.'}</p>
     {!canopyConfigured&&<p className="canopyConfigWarn">Canopy backend is not configured on this deployment yet. Add the Supabase environment variables before testing accounts.</p>}
    </div>
    <form onSubmit={submit}>
     {mode==='signup'&&<div className="canopyAuthTwoCol"><label>Full name<input required autoComplete="name" placeholder="Your full name" value={form.full_name} onChange={e=>setForm({...form,full_name:e.target.value})}/></label><label>Country<input required autoComplete="country-name" placeholder="Country" value={form.country} onChange={e=>setForm({...form,country:e.target.value})}/></label></div>}
     <label>Email address<input required type="email" autoComplete="email" placeholder="you@example.com" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label>
     <label>Password<div className="canopyPasswordField"><input required type={showPassword?'text':'password'} autoComplete={mode==='login'?'current-password':'new-password'} minLength="8" placeholder="Minimum 8 characters" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><button type="button" onClick={()=>setShowPassword(v=>!v)} aria-label={showPassword?'Hide password':'Show password'}>{showPassword?<EyeOff/>:<Eye/>}</button></div></label>
     <button className="canopyPrimary full canopyAuthSubmit" disabled={busy||!canopyConfigured}>{busy?'Please wait…':mode==='login'?'Sign in':'Create account'} <ArrowRight size={17}/></button>
     {msg&&<p className={`canopyFormMsg ${msgType}`} role="status" aria-live="polite">{msg}</p>}
     {mode==='login'&&<div className="canopyAuthHelp"><button type="button" disabled={busy} onClick={resend}>Resend confirmation</button><button type="button" disabled={busy} onClick={forgot}>Forgot password?</button></div>}
     <p className="canopySwitch">{mode==='login'?'New to Canopy?':'Already have an account?'} <button type="button" onClick={()=>go(mode==='login'?'/canopy/signup':'/canopy/login')}>{mode==='login'?'Create an account':'Sign in'}</button></p>
    </form>
   </section>
  </main>
 </div>
}
function AuthCallback(){const[msg,setMsg]=useState('Confirming your Canopy account…');useEffect(()=>{let live=true;(async()=>{try{const result=await consumeAuthCallback();if(!live)return;if(result?.type==='recovery'){setMsg('Password-reset link accepted. Opening secure password update…');setTimeout(()=>go('/canopy/reset-password'),350)}else{setMsg('Email confirmed successfully. Opening Canopy…');setTimeout(()=>go('/canopy/classroom'),350)}}catch(err){if(live)setMsg(err.message||'This confirmation link is invalid or has expired. Return to sign in and request a fresh confirmation email.')}})();return()=>{live=false}},[]);return <div className="canopyLoading"><Leaf/><span>{msg}</span><button className="canopySecondary" onClick={()=>go('/canopy/login')}>Return to sign in</button></div>}

function ResetPassword(){const[p1,setP1]=useState('');const[p2,setP2]=useState('');const[busy,setBusy]=useState(false);const[msg,setMsg]=useState('');const submit=async e=>{e.preventDefault();if(p1!==p2){setMsg('The passwords do not match.');return}setBusy(true);setMsg('');try{await updatePassword(p1);setMsg('Password changed successfully. You can now sign in with your new password.');setTimeout(async()=>{await signOut();go('/canopy/login')},700)}catch(err){setMsg(err.message)}finally{setBusy(false)}};return <div className="canopyAuth"><aside><Brand/><div><span className="canopyEyebrow">ACCOUNT RECOVERY</span><h1>Set a new password.</h1><p>Choose a new password for your WOMATE Canopy account.</p></div><blockquote>SECURE · CONTINUE · LEARN</blockquote></aside><main><button className="canopyBack" onClick={()=>go('/canopy/login')}><ArrowLeft/> Back to sign in</button><form onSubmit={submit}><div className="canopyAuthHead"><span>PASSWORD RESET</span><h2>New password</h2></div><label>New password<input required type="password" minLength="8" autoComplete="new-password" value={p1} onChange={e=>setP1(e.target.value)}/></label><label>Confirm new password<input required type="password" minLength="8" autoComplete="new-password" value={p2} onChange={e=>setP2(e.target.value)}/></label><button className="canopyPrimary full" disabled={busy}>{busy?'Updating…':'Change password'} <ArrowRight size={17}/></button>{msg&&<p className="canopyFormMsg" role="status">{msg}</p>}</form></main></div>}

function CourseNav({open,setOpen,viewer}){
 const manager=['manager','admin'].includes(viewer?.profile?.role);
 const learnerItems=[['/canopy/classroom','Home',BookOpen],['/canopy/course/she-leads','Course',GraduationCap],['/canopy/assignments','Assignments',FileText],['/canopy/progress','Progress',ClipboardCheck],['/canopy/resources','Resources',Sparkles],['/canopy/profile','Profile',UserRound],['/canopy/canvas','CanopyCanvas',PenLine],['/canopy/notifications','Notifications',MessageSquare],['/canopy/certificate','Certificates',Award]];
 const managerItems=[['/canopy/manage','Operations',ClipboardCheck],['/canopy/manage/access','Manage cohort',UsersRound],['/canopy/manage/reviews','Assess submissions',FileText],['/canopy/manage/communications','Warnings & feedback',MessageSquare],['/canopy/manage/reminders','Reminders',Bell],['/canopy/manage/complaints','Complaints',ShieldAlert],['/canopy/manage/certificates','Certificates',Award],['/canopy/manage/reports','Reports',BarChart3]];
 const items=manager?managerItems:learnerItems;
 return <aside className={'canopySidebar '+(open?'open':'')}>
  <div className="canopySideTop"><Brand compact/><button className="canopySideClose" onClick={()=>setOpen(false)}><X/></button></div>
  <nav>{items.map(([p,n,I])=><button key={p} className={route()===p?'active':''} onClick={()=>{go(p);setOpen(false)}}><I size={18}/><span>{n}</span></button>)}</nav>
  <div className="canopySideBottom"><small>{manager?'WOMATE OPERATIONS':CANOPY_BRAND.programme}</small><b>{manager?'She Leads Climate Mentorship · Cohort 2 · 2026':CANOPY_BRAND.cohort}</b></div>
 </aside>
}
function LearnerShell({viewer,progress,children,onReload}){
 const[open,setOpen]=useState(false);
 const manager=['manager','admin'].includes(viewer?.profile?.role);
 const name=viewer?.profile?.full_name||viewer?.user?.user_metadata?.full_name||viewer?.user?.email?.split('@')[0]||(manager?'WOMATE':'Learner');
 return <div className="canopyApp"><CourseNav open={open} setOpen={setOpen} viewer={viewer}/><div className="canopyMain"><header className="canopyAppTop"><button className="canopyMenu" onClick={()=>setOpen(true)}><Menu/></button><div><span>{manager?'WOMATE · CANOPY OPERATIONS':'She Leads Climate Mentorship · Cohort 2 · 2026'}</span><b>{name}</b></div><button className="canopySignout" onClick={async()=>{await signOut();go('/canopy/login')}}><LogOut size={17}/><span>Sign out</span></button></header>{children}</div></div>
}

function Dashboard({viewer,progress}){const done=new Set(progress.filter(x=>x.completed).map(x=>x.lesson_id));const completed=done.size;const pct=Math.round(completed/totalLessons*100);let next=null;for(const m of modules){for(const l of m.lessons){if(!done.has(l.id)){next={m,l};break}}if(next)break}const enrolled=viewer.enrollments?.some(e=>e.status==='active');return <main className="canopyDashboard"><section className="canopyWelcome"><div><span className="canopyEyebrow">MY CANOPY</span><h1>Grow through the work.</h1><p>{enrolled?'Your She Leads course is active. Continue from where you stopped.':'Your account is ready. Canopy learning access opens from 1 October 2026.'}</p></div><div className="canopyProgressRing" style={{'--p':`${pct*3.6}deg`}}><div><strong>{pct}%</strong><span>complete</span></div></div></section>{!enrolled?<section className="canopyLockedState"><Lock/><div><h2>Awaiting cohort enrolment</h2><p>Canopy learning access opens from 1 October 2026.</p></div></section>:<><section className="canopyContinue"><div><span>CONTINUE LEARNING</span><h2>{next?next.m.title:'Course learning complete'}</h2><p>{next?next.l.title:'Review your progress and final submissions.'}</p></div><button className="canopyPrimary" onClick={()=>next?go(`/canopy/course/she-leads/${next.m.id}/${next.l.id}`):go('/canopy/progress')}>{next?'Continue':'View progress'} <ArrowRight size={17}/></button></section><section className="canopyDashGrid"><article><small>COURSE</small><h3>She Leads Climate Mentorship</h3><Meta/><button onClick={()=>go('/canopy/course/she-leads')}>Open course <ArrowRight size={15}/></button></article><article><small>YOUR PROGRESS</small><h3>{completed} of {totalLessons} lessons complete</h3><div className="canopyLine"><span style={{width:`${pct}%`}}/></div><p>Short lessons, five knowledge checks and five applied assignments.</p></article></section></>}</main>}

function canopyModuleSchedule(m){return CANOPY_ASSIGNMENT_SCHEDULE.find(x=>x.moduleId===m.id)||null}
function canopyModuleIsOpen(m,now=new Date()){const item=canopyModuleSchedule(m);return !!item&&now>=new Date(item.weekStartsAt)}
function canopyModuleOpenLabel(m){const item=canopyModuleSchedule(m);if(!item)return 'Scheduled';return new Intl.DateTimeFormat('en-GB',{weekday:'short',day:'numeric',month:'short',timeZone:'UTC'}).format(new Date(item.weekStartsAt))}
function CourseOverview({progress}){const done=new Set(progress.filter(x=>x.completed).map(x=>x.lesson_id));return <main className="canopyCoursePage"><section className="canopyCourseHero"><div><span className="canopyEyebrow">SHE LEADS · {CANOPY_BRAND.cohort}</span><h1>From Climate Knowledge to Climate Leadership</h1><p>Five foundational modules designed to help emerging women leaders understand the climate challenge, participate in decision-making, communicate what matters and identify where they can go next.</p><Meta/></div><div className="canopyCourseStat"><strong>{modules.length}</strong><span>modules</span><strong>{totalLessons}</strong><span>short lessons</span></div></section><section className="canopyCurriculum"><div className="canopySectionHead"><span>CURRICULUM</span><h2>Your learning path</h2></div>{modules.map(m=>{const n=m.lessons.filter(l=>done.has(l.id)).length;const locked=!canopyModuleIsOpen(m);return <article key={m.id}><button className="canopyModuleRow" disabled={locked} onClick={()=>!locked&&go(`/canopy/course/she-leads/${m.id}/${m.lessons[0].id}`)}><span className="canopyModuleNo">{m.number}</span><div><small>{m.week}</small><h3>{m.title}</h3><p>{m.question}</p><span>{n}/{m.lessons.length} lessons complete</span></div><div className="canopyModuleArrow">{locked?<><span className="canopyModuleOpenDate">Opens {canopyModuleOpenLabel(m)}</span><Lock/></>:<ArrowRight/>}</div></button></article>})}</section></main>}

function YouTube({id,title}){if(!id)return <div className="canopyVideoPlaceholder"><PlayCircle/><div><b>Module video slot</b><span>Add this module’s unlisted YouTube video ID in your Vercel environment variables.</span></div></div>;return <div className="canopyVideo"><iframe src={`https://www.youtube-nocookie.com/embed/${id}?rel=0&modestbranding=1`} title={title} allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowFullScreen/></div>}
function Lesson({moduleId,lessonId,progress,session,reload}){const m=modules.find(x=>x.id===moduleId)||modules[0];if(!canopyModuleIsOpen(m))return <main className="canopyLockedModule"><Lock/><h1>This module opens with the cohort.</h1><p>Canopy releases learning week by week so everyone moves through the programme together.</p><button className="canopySecondary" onClick={()=>go('/canopy/course/she-leads')}>Back to course</button></main>;const index=m.lessons.findIndex(x=>x.id===lessonId);const lesson=m.lessons[Math.max(0,index)];const done=progress.some(x=>x.lesson_id===lesson.id&&x.completed);const next=m.lessons[index+1];const prev=m.lessons[index-1];const complete=async()=>{await markLesson(session,lesson.id,m.id,true);await reload();if(next)go(`/canopy/course/she-leads/${m.id}/${next.id}`)};return <main className="canopyLesson"><div className="canopyLessonCrumb"><button onClick={()=>go('/canopy/course/she-leads')}><ArrowLeft/> Course</button><span>Module {m.number}</span></div><div className="canopyLessonLayout"><aside><span>{m.number}</span><h2>{m.title}</h2><div>{m.lessons.map((l,i)=><button key={l.id} className={l.id===lesson.id?'active':''} onClick={()=>go(`/canopy/course/she-leads/${m.id}/${l.id}`)}><span>{progress.some(x=>x.lesson_id===l.id&&x.completed)?<Check/>:i+1}</span><b>{l.title}</b></button>)}<button className="quiz" onClick={()=>go(`/canopy/course/she-leads/${m.id}/quiz`)}><ClipboardCheck/><b>Knowledge check</b></button></div></aside><article><header><small>{m.week} · {lesson.minutes} MIN READ</small><h1>{lesson.title}</h1></header>{index===0&&<YouTube id={m.youtubeId} title={`${m.title} video`}/>}<div className="canopyLessonBody">{lesson.body.map((p,i)=><p key={i}>{p}</p>)}<div className="canopyTakeaway"><Leaf/><div><span>KEY TAKEAWAY</span><p>{lesson.takeaway}</p></div></div></div><footer><button className="canopySecondary" disabled={!prev} onClick={()=>prev&&go(`/canopy/course/she-leads/${m.id}/${prev.id}`)}><ArrowLeft/> Previous</button><button className="canopyPrimary" onClick={complete}>{done?'Completed':'Mark complete'} {next?<ArrowRight/>:<Check/>}</button></footer></article></div></main>}

function Quiz({moduleId,session,reload}){const m=modules.find(x=>x.id===moduleId)||modules[0];if(!canopyModuleIsOpen(m))return <main className="canopyLockedModule"><Lock/><h1>This module opens with the cohort.</h1><p>Knowledge checks become available with their scheduled module.</p><button className="canopySecondary" onClick={()=>go('/canopy/course/she-leads')}>Back to course</button></main>;const[answers,setAnswers]=useState({});const[result,setResult]=useState(null);const submit=async e=>{e.preventDefault();let score=0;m.quiz.forEach((q,i)=>{if(Number(answers[i])===q.answer)score++});setResult({score,total:m.quiz.length});try{await saveQuiz(session,m.id,score,m.quiz.length)}catch{}};return <main className="canopyQuiz"><button className="canopyBack" onClick={()=>go(`/canopy/course/she-leads/${m.id}/${m.lessons[m.lessons.length-1].id}`)}><ArrowLeft/> Back to module</button><section><span className="canopyEyebrow">MODULE {m.number} · KNOWLEDGE CHECK</span><h1>{m.title}</h1><p>Check your understanding. You can review the module and try again.</p><form onSubmit={submit}>{m.quiz.map((q,i)=><fieldset key={q.q}><legend>{i+1}. {q.q}</legend>{q.options.map((o,j)=><label key={o}><input type="radio" name={`q${i}`} value={j} checked={String(answers[i])===String(j)} onChange={e=>setAnswers({...answers,[i]:e.target.value})}/><span>{o}</span></label>)}</fieldset>)}<button className="canopyPrimary" disabled={Object.keys(answers).length!==m.quiz.length}>Check answers</button></form>{result&&<div className="canopyQuizResult"><strong>{result.score}/{result.total}</strong><div><h3>{result.score===result.total?'Strong work.':'Keep building the foundation.'}</h3><p>{result.score===result.total?'You answered every question correctly.':'Review any concepts you missed, then try again when ready.'}</p><button className="canopyTextBtn" onClick={()=>go('/canopy/assignments')}>Go to assignments <ArrowRight/></button></div></div>}</section></main>}

function Assignments({session,submissions,reload}){const[open,setOpen]=useState(null);const[text,setText]=useState('');const[busy,setBusy]=useState(false);const send=async()=>{if(!open||text.trim().length<80)return;setBusy(true);try{await submitAssignment(session,open.id,open.assignment.title,text.trim());setOpen(null);setText('');await reload()}finally{setBusy(false)}};return <main className="canopyAssignments"><div className="canopyPageHead"><span className="canopyEyebrow">APPLY THE LEARNING</span><h1>Assignments</h1><p>Each assignment is short, practical and connected to the module. Your final Climate Action Note turns the programme into a 90-day next step.</p></div><section>{modules.map(m=>{const sub=submissions.find(s=>s.module_id===m.id);return <article key={m.id}><span>{m.number}</span><div><small>{m.title}</small><h3>{m.assignment.title}</h3><p>{m.assignment.prompt}</p>{sub&&<b className="canopySubmitted"><Check/> Submitted · {sub.status}</b>}</div><button className="canopySecondary" onClick={()=>{setOpen(m);setText(sub?.response||'')}}>{sub?'View / resubmit':'Start assignment'}</button></article>})}</section>{open&&<div className="canopyModal"><button className="canopyModalBack" onClick={()=>setOpen(null)} aria-label="Close"/><div className="canopyModalPanel"><header><div><small>{open.title}</small><h2>{open.assignment.title}</h2></div><button onClick={()=>setOpen(null)}><X/></button></header><p>{open.assignment.prompt}</p><label>Your response<textarea value={text} onChange={e=>setText(e.target.value)} placeholder="Write your response here…"/></label><small>{text.trim().length} characters · minimum 80</small><button className="canopyPrimary" disabled={busy||text.trim().length<80} onClick={send}>{busy?'Submitting…':'Submit assignment'}</button></div></div>}</main>}

function Progress({progress,submissions}){const done=new Set(progress.filter(x=>x.completed).map(x=>x.lesson_id));const completed=done.size;const pct=Math.round(completed/totalLessons*100);return <main className="canopyProgressPage"><div className="canopyPageHead"><span className="canopyEyebrow">YOUR LEARNING RECORD</span><h1>Progress</h1><p>Your progress is based on completed lessons and submitted assignments.</p></div><section className="canopyProgressHero"><div><strong>{pct}%</strong><span>lesson completion</span></div><div><strong>{completed}/{totalLessons}</strong><span>lessons</span></div><div><strong>{submissions.length}/{modules.length}</strong><span>assignments submitted</span></div></section><section className="canopyProgressModules">{modules.map(m=>{const n=m.lessons.filter(l=>done.has(l.id)).length;const mp=Math.round(n/m.lessons.length*100);return <article key={m.id}><span>{m.number}</span><div><h3>{m.title}</h3><div className="canopyLine"><span style={{width:`${mp}%`}}/></div><small>{n}/{m.lessons.length} lessons · {submissions.some(s=>s.module_id===m.id)?'assignment submitted':'assignment pending'}</small></div><strong>{mp}%</strong></article>})}</section></main>}
function Resources(){return <main className="canopyResources"><div className="canopyPageHead"><span className="canopyEyebrow">KEEP LEARNING</span><h1>Resources</h1><p>Selected starting points for learners who want to continue beyond the foundational course.</p></div><section>{resources.map(r=><a key={r.url} href={r.url} target="_blank" rel="noreferrer"><FileText/><div><small>{r.type}</small><h3>{r.title}</h3></div><ArrowRight/></a>)}</section></main>}
function Profile({viewer}){const p=viewer.profile||{};return <main className="canopyProfile"><div className="canopyPageHead"><span className="canopyEyebrow">LEARNER PROFILE</span><h1>{p.full_name||viewer.user?.user_metadata?.full_name||'Your profile'}</h1><p>Your Canopy identity is connected to your WOMATE learning account.</p></div><section><div><small>Email</small><b>{viewer.user?.email}</b></div><div><small>Country</small><b>{p.country||viewer.user?.user_metadata?.country||'—'}</b></div><div><small>Role</small><b>{p.role||'learner'}</b></div><div><small>Course access</small><b>{viewer.enrollments?.some(e=>e.status==='active')?'Active':'Awaiting enrolment'}</b></div></section></main>}

function ManagerNotice({children}){return children?<p className="canopyFormMsg" role="status">{children}</p>:null}
function ManagerOperations({snapshot,session,onReload,view='overview'}){
 const[busy,setBusy]=useState('');const[msg,setMsg]=useState('');const[form,setForm]=useState({learner_id:'',action_type:'reminder',subject:'',message:''});
 if(!snapshot)return <main className="canopyManager"><div className="canopyPageHead"><h1>Canopy operations</h1><p>Loading operational data…</p></div></main>;
 const learners=snapshot.profiles.filter(p=>p.role==='learner');
 const actions=snapshot.actions||[];
 const enrolFor=id=>snapshot.enrollments.find(e=>e.user_id===id);
 const nameFor=id=>learners.find(p=>p.user_id===id)?.full_name||'Learner';
 const activate=async(id,status)=>{setBusy(id);setMsg('');try{await setLearnerEnrollmentStatus(session,id,status);setMsg(status==='active'?'Learner access activated.':'Learner access deactivated.');await onReload?.()}catch(e){setMsg(e.message)}finally{setBusy('')}};
 const sendAction=async e=>{e.preventDefault();if(!form.learner_id||!form.subject)return;setBusy('action');setMsg('');try{const extra=form.action_type==='certificate'?{status:'issued',certificate_code:'WOMATE-SL2-'+Date.now().toString(36).toUpperCase()}:{status:'open'};await createManagerAction(session,{...form,...extra});setForm({learner_id:'',action_type:'reminder',subject:'',message:''});setMsg(form.action_type==='certificate'?'Certificate issued to learner record.':'Operational message recorded and delivered inside Canopy.');await onReload?.()}catch(e){setMsg(e.message)}finally{setBusy('')}};
 const resolve=async(a,status='resolved')=>{setBusy(a.id);setMsg('');try{await updateManagerAction(session,a.id,{status,resolved_at:new Date().toISOString()});setMsg('Record updated.');await onReload?.()}catch(e){setMsg(e.message)}finally{setBusy('')}};
 const sectionTitle={overview:'Operations',access:'Manage cohort',reviews:'Assess submissions',communications:'Warnings & feedback',reminders:'Reminders',complaints:'Complaints',certificates:'Certificates',reports:'Reports'}[view]||'Operations';
 const filtered=type=>actions.filter(a=>a.action_type===type);
 return <main className="canopyManager canopyOps">
  <div className="canopyPageHead"><span className="canopyEyebrow">WOMATE · CANOPY</span><h1>{sectionTitle}</h1><p>{view==='overview'?'Operational control for She Leads Climate Mentorship · Cohort 2 · 2026.':'Manage this workstream from one authorised WOMATE account.'}</p></div>
  <ManagerNotice>{msg}</ManagerNotice>
  {view==='overview'&&<>
   <section className="canopyManagerStats"><article><strong>{learners.length}</strong><span>learners</span></article><article><strong>{snapshot.enrollments.filter(e=>e.status==='active').length}</strong><span>active access</span></article><article><strong>{snapshot.submissions.length}</strong><span>submissions</span></article><article><strong>{actions.filter(a=>a.status==='open').length}</strong><span>open actions</span></article></section>
   <section className="canopyOpsGrid">
    <button onClick={()=>go('/canopy/manage/access')}><UsersRound/><span><b>Learner access</b><small>Activate or deactivate participants.</small></span><ArrowRight/></button>
    <button onClick={()=>go('/canopy/manage/reviews')}><FileText/><span><b>Reviews</b><small>Assess submissions and learner progress.</small></span><ArrowRight/></button>
    <button onClick={()=>go('/canopy/manage/communications')}><MessageSquare/><span><b>Communications</b><small>Warnings, feedback and reminders.</small></span><ArrowRight/></button>
    <button onClick={()=>go('/canopy/manage/complaints')}><ShieldAlert/><span><b>Complaints</b><small>Record and resolve learner concerns.</small></span><ArrowRight/></button>
    <button onClick={()=>go('/canopy/manage/certificates')}><Award/><span><b>Certificates</b><small>Issue completion records.</small></span><ArrowRight/></button>
    <button onClick={()=>go('/canopy/manage/reports')}><BarChart3/><span><b>Reports</b><small>Monitor delivery and completion.</small></span><ArrowRight/></button>
   </section>
  </>}
  {view==='access'&&<section className="canopyManagerTable canopyManagerAccessTable"><header><b>Participant</b><b>Country</b><b>Access</b><b>Action</b></header>{learners.map(p=>{const e=enrolFor(p.user_id),active=e?.status==='active';return <div key={p.user_id}><span>{p.full_name||'Unnamed learner'}</span><span>{p.country||'—'}</span><span><b className={'canopyAccessStatus '+(active?'isActive':'isWaiting')}>{active?'Active':'Waiting'}</b></span><span><button className={active?'canopySecondary':'canopyPrimary'} disabled={busy===p.user_id} onClick={()=>activate(p.user_id,active?'inactive':'active')}>{busy===p.user_id?'Updating…':active?'Deactivate':'Activate access'}</button></span></div>})}</section>}
  {view==='reviews'&&<section className="canopyOpsList">{snapshot.submissions.length?snapshot.submissions.map(s=><article key={s.id||s.user_id+s.module_id}><div><small>{nameFor(s.user_id)} · {s.module_id}</small><h3>{s.title||'Assignment submission'}</h3><p>{s.response||'Submission recorded.'}</p></div><span className="canopyAccessStatus isWaiting">{s.status||'submitted'}</span></article>):<p>No submissions yet.</p>}</section>}
  {view==='communications'&&<><ManagerActionForm form={form} setForm={setForm} learners={learners} busy={busy} onSubmit={sendAction} types={['warning','feedback','reminder']}/><ManagerActionList actions={actions.filter(a=>['warning','feedback','reminder'].includes(a.action_type))} nameFor={nameFor} busy={busy} onResolve={resolve}/></>}
  {view==='complaints'&&<><ManagerActionForm form={{...form,action_type:'complaint'}} setForm={x=>setForm({...x,action_type:'complaint'})} learners={learners} busy={busy} onSubmit={sendAction} types={['complaint']}/><ManagerActionList actions={filtered('complaint')} nameFor={nameFor} busy={busy} onResolve={resolve}/></>}
  {view==='certificates'&&<><ManagerActionForm form={{...form,action_type:'certificate'}} setForm={x=>setForm({...x,action_type:'certificate'})} learners={learners} busy={busy} onSubmit={sendAction} types={['certificate']}/><ManagerActionList actions={filtered('certificate')} nameFor={nameFor} busy={busy} onResolve={resolve}/></>}
  {view==='reports'&&<section className="canopyOpsReport"><article><strong>{snapshot.progress.filter(x=>x.completed).length}</strong><span>completed lessons</span></article><article><strong>{snapshot.submissions.length}</strong><span>assignments submitted</span></article><article><strong>{filtered('certificate').filter(x=>x.status==='issued').length}</strong><span>certificates issued</span></article><article><strong>{filtered('complaint').filter(x=>x.status==='open').length}</strong><span>open complaints</span></article></section>}
 </main>
}
function ManagerActionForm({form,setForm,learners,busy,onSubmit,types}){
 const labels={warning:'Warning',feedback:'Feedback',reminder:'Reminder',complaint:'Complaint',certificate:'Certificate'};
 return <form className="canopyOpsForm" onSubmit={onSubmit}><div className="canopySectionHead"><span>NEW ACTION</span><h2>{types.length===1?labels[types[0]]:'Send learner communication'}</h2></div><div className="canopyOpsFormGrid"><label>Learner<select required value={form.learner_id} onChange={e=>setForm({...form,learner_id:e.target.value})}><option value="">Select learner</option>{learners.map(p=><option key={p.user_id} value={p.user_id}>{p.full_name||'Unnamed learner'}{p.country?' · '+p.country:''}</option>)}</select></label>{types.length>1&&<label>Type<select value={form.action_type} onChange={e=>setForm({...form,action_type:e.target.value})}>{types.map(t=><option key={t} value={t}>{labels[t]}</option>)}</select></label>}<label className="wide">Subject<input required value={form.subject} onChange={e=>setForm({...form,subject:e.target.value})} placeholder={types[0]==='certificate'?'She Leads Climate Mentorship · Cohort 2 · 2026':'Short operational subject'}/></label><label className="wide">Message<textarea value={form.message} onChange={e=>setForm({...form,message:e.target.value})} rows="5" placeholder="Add the relevant note, feedback or decision."/></label></div><button className="canopyPrimary" disabled={busy==='action'}>{busy==='action'?'Saving…':types[0]==='certificate'?'Issue certificate':'Save and send'} <ArrowRight size={16}/></button></form>
}
function ManagerActionList({actions,nameFor,busy,onResolve}){return <section className="canopyOpsList">{actions.length?actions.map(a=><article key={a.id}><div><small>{a.action_type.toUpperCase()} · {nameFor(a.learner_id)}</small><h3>{a.subject}</h3><p>{a.message||'No additional note.'}</p>{a.certificate_code&&<code>{a.certificate_code}</code>}</div><div className="canopyOpsListActions"><span className={'canopyAccessStatus '+(a.status==='open'?'isWaiting':'isActive')}>{a.status}</span>{a.status==='open'&&<button className="canopySecondary" disabled={busy===a.id} onClick={()=>onResolve(a)}>{busy===a.id?'Updating…':'Resolve'}</button>}</div></article>):<p>No records here yet.</p>}</section>}

const canopyHeroDemoSlides=['/assets/canopy/hero-demo/course-lesson.png','/assets/canopy/hero-demo/live-session.png','/assets/canopy/hero-demo/assignment.png','/assets/canopy/hero-demo/course-complete.png','/assets/canopy/hero-demo/course-catalogue.png','/assets/canopy/hero-demo/canopy-login.png'];

function CanopyHeroDemo(){
 const[slide,setSlide]=useState(0);
 useEffect(()=>{
  const timer=setInterval(()=>setSlide(i=>(i+1)%canopyHeroDemoSlides.length),5200);
  return()=>clearInterval(timer);
 },[]);
 return <section className="canopyHeroDemo" aria-label="Canopy learning platform preview">
  <div className="canopyHeroDemoStage">
   {canopyHeroDemoSlides.map((src,i)=><img key={src} src={src} alt="" aria-hidden={i!==slide} className={'canopyHeroDemoImage '+(i===slide?'isActive':'')}/>)}
  </div>
  <div className="canopyHeroDemoNav" aria-label="Preview slides">
   {canopyHeroDemoSlides.map((_,i)=><button key={i} type="button" className={i===slide?'isActive':''} aria-label={'Show preview '+(i+1)} onClick={()=>setSlide(i)}/>)}
  </div>
 </section>;
}

export default function CanopyApp(){
if(window.location.pathname.startsWith('/canopy/cantest'))return <CantestApp/>;
const[path,setPath]=useState(route());const[loading,setLoading]=useState(true);const[viewer,setViewer]=useState(null);const[progress,setProgress]=useState([]);const[submissions,setSubmissions]=useState([]);const[snapshot,setSnapshot]=useState(null);const load=async()=>{const session=getStoredSession();if(!session){setViewer(null);setProgress([]);setSubmissions([]);setSnapshot(null);setLoading(false);return}try{const v=await getViewer(session);if(!v){setViewer(null);return}setViewer(v);const results=await Promise.allSettled([getProgress(v.session),getSubmissions(v.session)]);setProgress(results[0].status==='fulfilled'?(results[0].value||[]):[]);setSubmissions(results[1].status==='fulfilled'?(results[1].value||[]):[]);if(results[0].status==='rejected')console.error('Canopy progress load failed:',results[0].reason);if(results[1].status==='rejected')console.error('Canopy submissions load failed:',results[1].reason);if(['admin','manager'].includes(v.profile?.role)){try{setSnapshot(await getManagerSnapshot(v.session))}catch(err){console.error('Canopy manager snapshot failed:',err);setSnapshot({profiles:[],enrollments:[],progress:[],submissions:[],actions:[],dataError:String(err?.message||err)})}}else setSnapshot(null)}catch(err){console.error('Canopy viewer load failed:',err);setViewer(null);setProgress([]);setSubmissions([]);setSnapshot(null)}finally{setLoading(false)}};useEffect(()=>{const fn=()=>setPath(route());window.addEventListener('popstate',fn);return()=>window.removeEventListener('popstate',fn)},[]);useEffect(()=>{setLoading(true);load();},[path]);const publicRoute=path==='/canopy'||path==='/canopy/';const authRoute=path==='/canopy/login'||path==='/canopy/signup';if(path==='/canopy/auth/callback')return <AuthCallback/>;if(path==='/canopy/reset-password')return <ResetPassword/>;if(publicRoute)return <><Helmet><title>WOMATE Canopy | Climate Learning</title><meta name="description" content="WOMATE Canopy is the learning environment for structured climate education and She Leads Climate Mentorship."/></Helmet><Landing/></>;if(authRoute)return <Auth mode={path.endsWith('signup')?'signup':'login'}/>;if(loading)return <div className="canopyLoading"><Leaf/><span>Opening Canopy…</span></div>;if(!viewer){go('/canopy/login');return null}const manager=['manager','admin'].includes(viewer.profile?.role);if(manager&&!path.startsWith('/canopy/manage')&&path!=='/canopy/profile'){go('/canopy/manage');return null}const active=viewer.enrollments?.some(e=>e.status==='active');let content;if(manager&&path.startsWith('/canopy/manage')){const sub=path.split('/').pop();const view=path==='/canopy/manage'?'overview':(sub==='reminders'?'communications':sub);content=<ManagerOperations snapshot={snapshot} session={viewer.session} onReload={load} view={view}/>}else if(path==='/canopy/classroom')content=<Dashboard viewer={viewer} progress={progress}/>;else if(!active&&!['/canopy/profile','/canopy/notifications','/canopy/certificate'].includes(path))content=<Dashboard viewer={viewer} progress={progress}/>;else if(path==='/canopy/course/she-leads')content=<CourseOverview progress={progress}/>;else if(path==='/canopy/canvas') content=<CanopyCanvas viewer={viewer}/>;
    else if(path==='/canopy/notifications') content=<CanopyNotifications viewer={viewer}/>;
    else if(path==='/canopy/certificate') content=<CanopyCertificate viewer={viewer}/>;
    else if(path==='/canopy/assignments') content=<CanopyAssignmentsV2 viewer={viewer}/>;
    else if(path==='/canopy/assignments-legacy')content=<Assignments session={viewer.session} submissions={submissions} reload={load}/>;else if(path==='/canopy/progress')content=<Progress progress={progress} submissions={submissions}/>;else if(path==='/canopy/resources')content=<Resources/>;else if(path==='/canopy/profile')content=<Profile viewer={viewer}/>;else{const match=path.match(/^\/canopy\/course\/she-leads\/([^/]+)\/([^/]+)$/);if(match){const[,moduleId,last]=match;content=last==='quiz'?<Quiz moduleId={moduleId} session={viewer.session} reload={load}/>:<Lesson moduleId={moduleId} lessonId={last} progress={progress} session={viewer.session} reload={load}/>}else content=<Dashboard viewer={viewer} progress={progress}/>}return <><Helmet><title>My Canopy | WOMATE</title></Helmet><LearnerShell viewer={viewer} progress={progress}>{content}</LearnerShell></>}






